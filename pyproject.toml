[project]
name = "prospection-ia"
version = "0.1.0"
requires-python = ">=3.10,<3.15"
dependencies = [
    "beautifulsoup4",
    "ollama",
    "pandas",
    "playwright",
    "requests",
    "openpyxl",
]
